import React from 'react';
import AppScreensCarousel from './AppScreensCarousel';

export default function DesktopPhoneMockup({ mousePosition }) {
  return <AppScreensCarousel mousePosition={mousePosition} />;
}